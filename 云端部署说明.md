# Ubuntu 云端部署

目标服务器：Ubuntu，2 vCPU / 2 GB RAM / 40 GB，公网 IP `134.175.19.111`。

## 快速步骤

1. 在 DuckDNS 将 `superquant-2026.duckdns.org` 指向 `134.175.19.111`。
2. 将本目录（不含 `.venv`、`logs`、`reports`、`data/stocks`）上传到 `/opt/super-agent`。
3. 复制 `super-agent.env.example` 为 `/etc/super-agent/super-agent.env`，按需填写 Server酱密钥并执行 `chmod 600`。
4. 将 `Caddyfile.example` 中的 `CALLBACK_PATH` 替换为随机长路径后复制到 `/etc/caddy/Caddyfile`。
5. 安装 Python 依赖、Caddy、UFW，安装两个 systemd 单元并启动。

8000 只监听本机；公网只开放 SSH、80、443。UUMit 回调地址使用：
`https://superquant-2026.duckdns.org/CALLBACK_PATH`

部署完成并从公网验证 `/health`、`/analyze` 后，再在 UUMit 更新 callback_url。
