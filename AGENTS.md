# Super-agent 因果与验证规则

- 所有技术指标、评分和信号在日期 T 只能读取 T 及之前的数据。
- 收盘信号只能在下一可交易日开盘成交；停牌顺延，末尾订单不得补成交。
- 止损跳空按开盘价成交，盘中触发按止损价成交。
- 禁止负向 `shift`、未来 `iloc/iat`、`rolling(center=True)` 和信号链路中的 `bfill`。
- 参数候选必须使用 `wf-v3` 三折以上有效滚动样本外验证；最后126个交易日保持封存。
- `wf-v3` 固定为扩展窗口：初始训练252个交易日、purge 20日、验证63日、embargo 5日；每折独立优化和初始化账户；无该市场状态可入场日的折只保留审计记录，不参与折数和指标计算。
- 牛、熊、震荡状态各至少10笔样本外平仓，总计至少40笔；至少三分之二折超额夏普为正。
- 候选总体夏普须较基准提升至少10%，收益不低于基准，回撤不超过10%且不劣于基准。
- 同一验证日期区间只能消费一次；协议升级不得解锁旧区间；候选身份必须绑定代码、参数、开发期数据和股票池哈希。
- 封存区间读取前必须登记占用，失败或中断后不得复用；代码或参数变化必须等待新的未见数据。
- 历史时点股票池不完整时只能输出研究结果，不得进入影子或发布阶段。
- 影子候选与基准必须同起点、同资金、同股票池、同权益日期；至少20笔平仓且收益、夏普、回撤全部通过才可建议切换。
- 每日优化只用于研究；只有月度全状态候选可进入验证账本。
- 禁止自动覆盖 `data/optimal_params.json`，候选和影子结果必须写入独立目录。
- 修改策略、指标、评分、回测、候选或风控后必须运行 `python scripts/quality_gate.py --full`。
- 不得在同一变更中放宽门槛或更新回归基准来掩盖失败；旧回测收益均视为无效。
