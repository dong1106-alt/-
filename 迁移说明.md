# 换服务器迁移说明

代码已在 GitHub 私有仓库 `dong1106-alt/-`（分支 `snapshot/current-source`），所以迁移不再需要手工传文件（也就不会再出现中文文件名损坏的问题）。真正要搬的是**运行数据、密钥**，并更新**域名指向新 IP**。

## 当前架构速览

| 项 | 位置/取值 |
|---|---|
| 代码 | GitHub 私有仓库 `dong1106-alt/-`，`current` 软链指向 `releases/<版本>` |
| 运行数据 | `/opt/super-agent/data`（约 55MB：持仓/交易/信号/候选/任务状态/市场状态/K线缓存） |
| 运行时目录 | `/opt/super-agent/{logs,reports,.venv}`，通过 release 内软链外挂 |
| 密钥 | `/etc/super-agent/super-agent.env`（API 配置 + ServerChan） |
| 服务 | `super-agent-api`(8000)、`super-agent-scheduler`、`caddy`(80/443) |
| 域名 | `superquant-2026.duckdns.org`，DuckDNS A 记录指向服务器公网 IP |

## 推荐方式：新服务器跑 `migrate_server.sh`

该脚本在新服务器上执行，会自动完成：装依赖 → 拉代码（GitHub token 或从旧服务器 rsync）→ 建 release/current 结构 → 建虚拟环境 → 搬密钥和 Caddy 配置 → 装 systemd 服务 → 搬运行数据 → 放行防火墙。

### 准备

1. 准备一个 GitHub 只读 token（Fine-grained PAT，仅勾选仓库 `-` 的 Contents: Read-only；迁移后可撤销）。
2. 旧服务器 SSH 地址，例如 `ubuntu@134.175.19.111`。

### 执行（在新服务器上）

```bash
# 用 GitHub 拉代码（推荐）
OLD_HOST=ubuntu@134.175.19.111 \
GITHUB_TOKEN=你的只读token \
VERSION=20260101-migrated \
bash scripts/migrate_server.sh

# 或者：不配 GitHub token，代码也从旧服务器 rsync
OLD_HOST=ubuntu@134.175.19.111 bash scripts/migrate_server.sh
```

### 迁移后必须手动完成的网络改动

1. **更新 DuckDNS A 记录**到新 IP（token 别提交）：
   ```bash
   curl "https://www.duckdns.org/update?domains=superquant-2026&token=$DUCKDNS_TOKEN&ip=新IP"
   ```
2. 等 DNS 生效后验证 HTTPS：
   ```bash
   curl -I https://superquant-2026.duckdns.org/health
   ```
3. 若 UUMit 回调写死了旧 IP，改回域名或新 IP；用域名则无需改。

### 验证

```bash
systemctl status super-agent-api super-agent-scheduler caddy
curl -fsS http://127.0.0.1:8000/health
# 等 15:00 后看当天日报，确认扫描/模拟/巡检/优化都 RUN_OK
```

## 回滚

迁移不改旧服务器。新服务器有问题时，老服务器仍可继续跑；只需把 DuckDNS 指回旧 IP 即可秒回滚。

## 注意事项

- 新服务器建议 Ubuntu 22.04（Python 3.10，与现有 `.venv` 一致）。同架构时脚本会优先复用已 rsync 的 `.venv`，否则自动 `pip install -r requirements.txt`。
- 2核4G 及以上即可；4G 对加载 4457 只股票的扫描和月度重优化更稳。
- 运行数据 `data/` 必须搬，否则会丢失模拟盘持仓/净值/任务状态，并需要重新回填全市场 K 线。
